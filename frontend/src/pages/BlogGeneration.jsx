import { useState } from "react";
import { Download, Copy, RotateCcw } from "lucide-react";
import toast from "react-hot-toast";

const BlogGeneration = () => {
  return <div>Blog Generation Page</div>;
};

export default BlogGeneration;
